<p style="color: green"><%= notice %></p>
<div class="row">
  <% @books.each do |book| %>
    <%= render partial: 'bookIn', locals: { book: book } %>
  <% end %>
</div>
<%= link_to "New book", new_book_path %>
